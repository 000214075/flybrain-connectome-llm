【自动循环 · 第 {{ROUND}} 轮 · 由 Stop 钩子/调度器自动发送，不是用户手打的】

你要做的第一件事是搞清楚"现在到哪一步了"，而不是从头开始：

1. 先读 `reports/FINAL_REPORT.md`（第 0 节与 §3.3 / §7.5 是两个模型的分工与实测数字），
   再列一次 `checkpoints/`、`configs/`、`scripts/`、`reports/` 的当前内容。
2. 找出**尚未完成或尚未验证**的事项。已知的未完成项至少包括：
   - 纯连接组模型（`arch: "connectome"`）当前最佳在 `checkpoints/flybrain-connectome/best.pt`：
     **lr 6e-5、804 步 / 40.04 分钟**撞墙钟上限而停，**曲线仍在下降、并未收敛**
     （固定 24 批打分 val_ppl **180.4**）。
   - **调度表与墙钟的对齐：已测，阴性结果，不要再测**（§7.9）。
     `max_steps: 800`（让退火在墙钟到点时走完）与现行的 `1200`（走不完、
     结束时 lr 还有 2.48e-5）**没有可测量差别**：固定 24 批 5.1928 vs 5.1949，
     差 0.0021 = seed 散布 0.0395 的 5%，且两个判据符号相反。
     这里面藏着一个陷阱值得记住：**800 步表在从第 100 到第 750 的每一个中间步数
     都更好**（均值 −0.0357、最好 −0.1367），**却在终点输掉**——因为短表退火更快，
     中间点的差距衡量的是"谁退火快"而不是"谁最终更好"。
     **不同调度表之间只有终点算数。**
   - **结论：调参已经榨干了。** 三处优化合计把同一预算下的困惑度从 274.6 降到 180.4
     （−34.3%），而最近这一项掉进噪声。剩余候选（1e-4 在完整预算下、
     长训练下的接线对照、蘑菇体三臂对照）预期也是这个量级。
     **真正的杠杆是算力**：40 分钟只能看 82 万 token = 语料的 2.0%
     （混合模型同预算约 2000 万）。要再下一个数量级，只有
     （a）加时间、（b）内核级降低每 token 扫描代价、（c）把连接组时间步与 token 解耦
     （需重新论证因果性）。**不要再花轮次去找超参收益。**
   - 它的语言质量远低于混合模型（同一留出集 val_ppl 180.4 vs 10.42），输出不成句；
   - 学习率：**已经测清并已在完整预算上验证完毕**（§7.8）：
     第 200 步定选择（3e-5 → 6.2252、**6e-5 → 5.9799**、1e-4 → 6.0130、2e-4 → 6.5032），
     然后把 6e-5 跑满 40 分钟 → 804 步、固定 24 批 **5.1949 / 180.4**；
     3e-5 同预算是 **5.3484 / 210.3**，同步数 16 点里 15 点 6e-5 更好（均值 −0.1540）。
     旧的"3e-5 上限"是恒定学习率探针的假象。**不要再做学习率探针**；
     仍未试的是 **1e-4 在完整预算下**（第 200 步与 6e-5 不可分辨）；
   - 两个扫描内核（CSR 与 `index_add`）**已经测清、不要再重测**（§3.3.5）：
     单次扫描差 1e-7（fp32 舍入）；在**同为 `max_steps: 600` 调度表**的两条臂上
     跑 450 步后验证损失平均只差 **+0.0013、最大 0.0023**（换 seed 散布 0.0395 的 3%），
     即换内核不改变模型学到什么。**先前"轨迹会在约 100 步后分开"的结论已撤回**——
     那次对照的两条臂 `max_steps` 不同（600 vs 150），第 150 步 lr 2.8e-5 vs 6e-6，
     差距主要来自退火差异。**不要再做 `brain_edge_chunk` 的确认实验**，那个问题已经答完；
   - 整脑接线的两臂对照（真实 / 保度数打乱）已在 §7.6 跑完：同配置、同 seed、同数据顺序、
     同一步数下，两条臂差 0.001–0.002。但两条臂都只跑到 150 步、远未收敛，
     所以"长训练下真实接线是否优于打乱接线"**仍未判定**，需要两条臂都跑到曲线拐弯之后；
   - 蘑菇体三臂对照（`train_mb.json` / `train_mb_shuffled.json` / `train_swiglu.json`）仍未跑；
   - 本项目的 run-to-run 波动**已经测过了**（报告 §7.7）：同 seed 重跑只差
     0.0001–0.0002（管线在给定 seed 下几乎逐位可复现），换 seed 时同一份固定
     24 批上的散布是 0.0395、训练内各自窗口是 0.2093。据此，接线对照的 0.0016
     只有它的 0.04 倍。**不要再重复测量**。仍未验证的是"800 步时散布是否同量级"。
     同一次排查还查出：**内核那条对照（§3.3.5）不是同步调度**——index_add 臂
     `max_steps: 600`、csr 臂 `max_steps: 150`，第 150 步 lr 2.8e-5 vs 6e-6。
     这一条**已经用两条同为 600 步调度表的臂补测完毕**（450 步平均差 +0.0013），
     **不要再重做**。教训保留：**同步数不等于同调度表**，
     任何跨运行对比都要先核对两条臂的 `max_steps` 是否相同；
   - GPU 利用率与"整脑 100% 参与"的指标可以继续往上推。
     已经做完的：扫描内核已从 `index_add` 换成 CSR（356 vs 218 tok/s，1.63 倍），
     **不要再重复测量或重复切换这一项**。
3. **只推进未完成或未验证的部分。** 已经做完并验证过的事不要重做：
   不要重新下载连接组或语料、不要重新生成教师语料、不要重跑已经跑完并已写入报告的实验。

本轮的硬性约束（违反这些比不做更糟）：
- **跑任何训练之前确认 C 盘剩余空间 ≥ 15 GB**（`Get-PSDrive C`）。每个检查点约
  1.37 GB、每次训练写 best+last 约 2.75 GB，而循环会不断产出新训练。
  空间不足时**停下来在报告里说明，不要自行删除 checkpoint**——删除是不可逆的，
  而且哪一份还有用需要人来判断。
- **跑任何训练或吞吐基准之前，先确认显卡是空的**（`Get-Process python`）。
  尤其不要在 `flybrain.serve` 还开着的时候跑训练：实测服务器占着卡时，
  真实接线训练从 218 tok/s 掉到 **51 tok/s**（4 倍以上），
  会让"墙钟上限"下的步数少 3 倍，进而得出完全错误的结论。
  跨运行比较任何墙钟/吞吐数字之前都要重新确认这一点。
  （本条是本项目第三次栽在"测量方式本身"上，前两次见报告 §3.3.1 与 §3.3.4。）
- 任何长任务都必须带墙钟上限（`max_minutes` 或 `--max-minutes`），并且**不允许空等**；
  用后台任务 + 完成通知，不要在前后台之间反复 sleep 轮询。
- 每次改动后跑 `pytest tests -q`，全部通过才算完成。
- 数字只能来自实测。没测过的就写"未验证"，不要估计、不要外推、不要把设计意图写成实测结果。
- 对照实验必须在**同一步数**上比较（墙钟上限会让两条臂步数不同，步数不同就不可横比）。
- 网页相关改动：本机没有浏览器工具，只能验证 HTTP/SSE 这一层，必须如实说明"浏览器渲染未验证"。
- 改完把结论回写 `reports/FINAL_REPORT.md`，让下一轮能接上。

原始要求（逐字保留，作为北极星，不要偏离）：
请你实现：联网搜索下载部署在本电脑中开源项目苍蝇大脑神经元，利用我提供的deepseek api来训练苍蝇大脑来实现训练一个基于苍蝇大脑神经元的llm项目要包含完整的训练流程和llm交互功能，要最高限度的充分利用amd显卡的性能，使用本amd显卡直接最高效的运行方式来搭建利用训练使用整个amd生态框架，在利用本电脑显卡硬件达成目标之前不停止
不仅限于本gpu利用率至100%，还要推进实现整个苍蝇大脑在本llm和llm输出的整个步骤都推进至100%，要绝对确保本gpu完整运行整个苍蝇大脑
需要穷尽一切手段把苍蝇大脑llm利用率推进至100%
请你确保最终训练的模型是完全基于苍蝇大脑结构构成的，没有新增任何有关于现代llm相关的分支依赖（需要颠覆传统llm）

本轮只做**一件**最有价值的事，做完就停，然后如实报告三件事：
做了什么、实测数字是多少、哪些没有验证。
